# Skippr ReAct

Skippr ReAct is a suite-neutral ReAct host and runtime. Skippr Data Engineer (`sde`) depends on these crates.

**Source-available** under PolyForm Shield 1.0.0, not OSI open source.

```bash
# git dependency (public)
# react = { git = "https://github.com/skipprd/react" }
```

CodeArtifact `react-cargo` is not the required public install path.

Used by: [Skippr Data Engineer](https://data-engineer.skippr.io)
