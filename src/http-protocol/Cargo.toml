[package]
name = "react-http-protocol"
version = "1.3.0"
edition = "2021"
description = "Private Skippr React crate: react-http-protocol"
license = "Apache-2.0"
publish = ["react-cargo"]

[dependencies]
serde = { version = "1.0.105", features = ["derive"] }
serde_json = "1.0.85"
