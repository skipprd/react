Patch required on the four staging models. (1) `stg_raw_trip_start`, `stg_raw_trip_pause`, `stg_raw_trip_resume`, and `stg_raw_trip_end` emit extra non-spec columns in their final `SELECT`, which changes the staged contract and can break downstream consumers expecting only the approved silver fields; restrict each final projection to `implementation_spec.output_fields` only. (2) `stg_raw_trip_pause` and `stg_raw_trip_resume` do not preserve `event_date_raw` unchanged as required, because they derive it with `NULLIF(TRIM(EVENT_DATE), '')`; pass `EVENT_DATE` through unchanged into `event_date_raw`. (3) All four models derive `event_at` / `event_at_parse_failed` from trimmed/nullified `EVENT_DATE` values rather than the spec expressions on `EVENT_DATE` itself, which causes whitespace-populated values to be treated as not failed and understates the data quality flag; align both expressions to the approved contract using `EVENT_DATE` directly.