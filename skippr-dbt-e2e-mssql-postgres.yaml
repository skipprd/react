project: el_mssql_postgres_e2e

warehouse:
  kind: postgres
  database: testdb
  schema: public

source:
  kind: mssql
  connection_string: ${MSSQL_CONNECTION_STRING}

dbt:
  target_schema: el_mssql_postgres_e2e
